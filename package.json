{
  "name": "cloudflare-email-sender",
  "version": "1.0.0",
  "description": "Cloudflare Workers 邮件发送程序",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "dev:ui": "wrangler dev --config wrangler.json",
    "dev:api": "wrangler dev --config wrangler-api.json",
    "deploy": "wrangler deploy",
    "deploy:ui": "wrangler deploy --config wrangler.json",
    "deploy:api": "wrangler deploy --config wrangler-api.json"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "email"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "mimetext": "^3.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
